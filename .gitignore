# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
xcuserdata/
DerivedData/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Built app bundle
Monarch.app/
Monarch *.app/
*.dSYM/

# Release artifacts — built by `ditto` at release time and attached to the
# GitHub release, so the local copy is redundant once the release is cut.
Monarch-*.zip

# Claude local settings (contains user-specific paths)
.claude/settings.local.json

# Private project notes
CLAUDE.md
TODO.md
AGENTS.md
EXCHANGE.md
VERIFY.md
